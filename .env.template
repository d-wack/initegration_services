# General Settings
LOG_LEVEL=INFO
ENVIRONMENT=development

# Webhook Service Settings
WEBHOOK_DB_TYPE=sqlite  # Options: sqlite, postgres, mysql
WEBHOOK_DB_URL=sqlite:///data/webhooks.db
WEBHOOK_QUEUE_TYPE=database  # Options: database, redis
WEBHOOK_SERVICE_PORT=8000

# OAuth Service Settings
OAUTH_DB_TYPE=sqlite  # Options: sqlite, postgres, mysql
OAUTH_DB_URL=sqlite:///data/oauth.db
OAUTH_ENCRYPTION_KEY=  # Required: Generate a secure encryption key
OAUTH_SERVICE_PORT=8001

# Logger Service Settings
LOG_STORAGE_TYPE=file  # Options: file, database, grafana, cloudwatch
LOG_DB_URL=sqlite:///data/logs.db
LOG_FILE_PATH=/data/logs
GRAFANA_API_KEY=
GRAFANA_CLOUD_URL=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
LOGGER_SERVICE_PORT=8002

# Integration Service Settings
MONDAY_API_URL=https://api.monday.com/v2
HUBSPOT_API_URL=https://api.hubspot.com/
INTEGRATION_SERVICE_PORT=8003

# Database Settings (for PostgreSQL or MySQL)
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# Redis Settings (if using Redis for queue)
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=

# SSL Settings
SSL_CERTIFICATE_PATH=
SSL_KEY_PATH=

# Security Settings
API_KEY_EXPIRY_DAYS=30
JWT_SECRET_KEY=  # Required for API authentication
CORS_ALLOWED_ORIGINS=http://localhost:*

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_PERIOD=60  # seconds 